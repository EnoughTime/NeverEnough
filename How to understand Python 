To understand Python, we must first understand its syntax. 

Python is an interpreted programming language. Its syntax uses indentation to define blocks of code, and some of its fundamental concepts include variables, loops, and conditionals.

Example:

v = 1
if v == 1:
    print("Hello")